"""
App_DocGraph User Plugins Package
=================================

This package contains user-installed plugins and extensions.

Scope
-----
User-level extensions only. These plugins must never modify
the core application under `system/`.

Plugins are discovered using:

    user/plugins/user_plugins_registry.json

Each plugin should provide a `plugin_manifest.json`.
"""