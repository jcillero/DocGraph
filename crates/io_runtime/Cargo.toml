[package]
name = "io_runtime"
version = "0.1.0"
edition = "2021"

[dependencies]
core_domain = { path = "../core_domain" }
workspace_core = { path = "../workspace_core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
