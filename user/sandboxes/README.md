# User sandboxes

Future authorized local sandboxes are external folders selected by the user.

No sandbox is authorized by default. Mutable actions remain disabled in the current F9-ready governance state.
