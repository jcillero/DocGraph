# LLM engines runtime placeholder

This directory is reserved for future materialized LLM engine descriptors or binaries.

No engine is installed or executable in the current F9-ready governance state.
