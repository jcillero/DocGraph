[package]
name = "document_text_runtime"
version = "0.1.0"
edition = "2021"

[dependencies]
core_domain = { path = "../core_domain" }
lopdf = "0.32"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
zip = { version = "0.6", default-features = false, features = ["deflate"] }
